extends layout

block content
  h1= title
  p Welcome to the page #{title}
  p Prasanna Yalla 
  if (!user)
    a(href="/login") Login
    br
    a(href="/register") Register
  if (user)
    p You are currently logged in as #{user.username}
    a(href="/grapes") Take me to grapes
    br
    a(href="/logout") Logout
  br
  button(type="button", class="btn btn-secondary" onclick='update()') press me 
  br
  br
  div(id='target') Display
  br
  a(href='/grapes') Search grapes page
  script.
    var client_count = 0;
    function update(){
      client_count++
      document.getElementById("target").innerHTML = `Value now ${client_count}`
    }